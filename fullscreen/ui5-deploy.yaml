# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "3.1"
metadata:
  name: student00i.com.sap.training.ux402.fullscreen.fullscreen
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: https://s4-ka1.kasolution.com.br
          client: '395'
        app:
          name: ZUX402CUSTCT00I
          description: Aplicativo UiLib
          package: $TMP
          transport: ''
        exclude:
          - /test/
